import React from 'react';

const ItemContainer = () => {
    return (
        <section className="pos-itemcontainer">
            Item Container
        </section>
    );
};

export default ItemContainer;
