import React from 'react';

const ItemContainer = () => {
    return (
        <section className="pos-item-container">
            Item Container
        </section>
    );
};

export default ItemContainer;
