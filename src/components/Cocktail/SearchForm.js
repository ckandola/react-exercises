import React from 'react';
import { useGlobalContext } from './Cocktail';

const SearchForm = () => {
    return (
        <div>
            <h2>SearchForm</h2>
        </div>
    );
};

export default SearchForm;
